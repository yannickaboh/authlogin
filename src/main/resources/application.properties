# Application configuration
# You can override any value with environment variables using uppercase + underscore, e.g.
# DB_URL, DB_USER, DB_PASS, EMAIL_USER, EMAIL_PASS

# Database configuration
db.url=jdbc:postgresql://localhost:5432/auth_system
db.user=auth_user
db.pass=QWzx1234@

# Email configuration (SMTP)
email.host=smtp.gmail.com
email.port=587
email.auth=true
email.starttls=true
